export enum LoanType {
  WEEKLY = "WEEKLY",
  MONTHLY = "MONTHLY",
}

export enum LoanStatus {
  OPEN = "OPEN",
  CLOSED = "CLOSED",
}

export enum PaymentType {
  INTEREST = "INTEREST",
  PRINCIPAL = "PRINCIPAL",
  EMI = "EMI",
}
